-- PalworldTuner config -- MULTIPLIERS on live 1.0 values. 1.0 = vanilla. Restart after changing.
return {
    carry_weight_mult = 1.0,
    tech_point_mult   = 1.0,
    -- Keep sprint proportional to the effective walk speed after perks/items.
    sprint_speed_sync = false,
    sprint_multiplier = 1.65,
    round_up          = true,
    log               = false,
}
